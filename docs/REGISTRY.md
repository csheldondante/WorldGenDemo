# Runtime Registry

> **This file is generated by `npm run registry`. Do not edit by hand.**
>
> Source of truth: `src/buffers/` and `src/systems/`. Re-run the script
> after adding or modifying any buffer or system.

## Buffers

| id | description | written by | read by |
| --- | --- | --- | --- |
| `input` | Raw input device state: held keys, accumulated mouse deltas, pointer-lock state, gamepad axes + buttons. Mouse deltas are drained each tick by InputMapperSystem. No gameplay system should read this directly — read InputMapBuffer instead. | `inputSystem`, `inputMapperSystem` | `inputSystem`, `inputMapperSystem` |
| `inputMap` | Device-agnostic semantic input: clamped move axis, accumulated look delta (radians/tick), edge-detected named actions. Written by InputMapperSystem; read by gameplay consumers (CharacterInputSystem, CameraFollowSystem). No gameplay system should read InputBuffer directly — read this instead. | `inputMapperSystem` | `inputMapperSystem`, `hudSystem`, `characterInputSystem`, `characterOrientationSystem`, `cameraOrbitSystem` |
| `camera` | Source-of-truth camera state. Renderer-facing fields (pos/yaw/pitch/fov/aspect/near/far) are mirrored into THREE.PerspectiveCamera by RenderSystem. Pivot (position/up/fwd) is the orbit centre, written by CameraPivotSystem from the character + local gravity. Target spherical coords (distance/yaw/pitch/roll/fov) + params (responsiveness, pitch cushion, FOV default) drive CameraOrbitSystem's chase. | `assetPlacementSystem`, `cameraPivotSystem`, `cameraOrbitSystem` | `renderSystem`, `minimapSystem`, `assetPlacementSystem`, `characterInputSystem`, `characterOrientationSystem`, `cameraPivotSystem`, `cameraOrbitSystem` |
| `events` | FIFO event queue drained each tick by StateMachineSystem. | `stateMachineSystem`, `loadSceneSystem`, `assetPlacementSystem`, `builderSystem` | `stateMachineSystem`, `loadSceneSystem`, `assetPlacementSystem`, `builderSystem` |
| `stateMachine` | Active runtime state + the graph id the scheduler should run this tick. | `stateMachineSystem` | `stateMachineSystem`, `loadSceneSystem`, `parseBitmapSystem`, `splitLayersSystem`, `jfaSystem`, `heightmapSystem`, `terrainMeshSystem`, `assetPlacementSystem`, `surfaceProviderSystem`, `parametricSurfaceSystem`, `playerSpawnSystem`, `builderSystem` |
| `renderRefs` | Three.js handles + DOM refs. Three.js is a render backend; gameplay state lives in other buffers. | `minimapSystem`, `terrainMeshSystem`, `assetPlacementSystem`, `parametricSurfaceSystem` | `renderSystem`, `minimapSystem`, `hudSystem`, `jfaSystem`, `terrainMeshSystem`, `assetPlacementSystem`, `parametricSurfaceSystem`, `characterRenderSyncSystem`, `skeletonDebugRenderSystem` |
| `worldData` | Per-scene data: source bitmap, parsed maps, heightmap, JFA outputs. Each pipeline stage writes its slice. | `parseBitmapSystem`, `splitLayersSystem`, `jfaSystem`, `heightmapSystem`, `parametricSurfaceSystem` | `minimapSystem`, `splitLayersSystem`, `jfaSystem`, `heightmapSystem`, `terrainMeshSystem`, `assetPlacementSystem`, `surfaceProviderSystem`, `parametricSurfaceSystem`, `playerSpawnSystem`, `builderSystem` |
| `timing` | Per-stage timing metrics + warnings. Read by HudSystem; written by every system that times itself. | `loadSceneSystem`, `parseBitmapSystem`, `splitLayersSystem`, `jfaSystem`, `heightmapSystem`, `terrainMeshSystem`, `assetPlacementSystem`, `surfaceProviderSystem`, `parametricSurfaceSystem`, `playerSpawnSystem`, `builderSystem` | — |
| `builder` | Mode-bounded editor state: bitmap, palette of {kind,id,color} entries, active brush, undo history, thumbnails. Lifetime is the editor session, not per-scene. | `builderSystem` | `builderSystem` |
| `entity` | Lightweight ECS: entities are integer IDs in `alive`; components live in keyed buffers (transform, velocity, characterController, etc). | `playerSpawnSystem` | `playerSpawnSystem` |
| `transform` | Per-entity world position, yaw, scale. Source of truth for character + asset placement. | `playerSpawnSystem`, `characterOrientationSystem`, `surfaceConstrainedVelocitySystem`, `volumetricConstrainedVelocitySystem`, `surfaceConstraintSystem` | `playerSpawnSystem`, `characterOrientationSystem`, `forceFieldSystem`, `characterControllerSystem`, `surfaceConstrainedVelocitySystem`, `volumetricConstrainedVelocitySystem`, `surfaceConstraintSystem`, `cameraPivotSystem`, `characterRenderSyncSystem`, `bodyLeanSystem`, `chainDynamicsSystem`, `footPlannerSystem`, `footIkSystem`, `skeletonWorldSystem` |
| `velocity` | Per-entity world-space linear velocity (m/s). VelocityIntegrationSystem updates Transform from this each tick; snapshots prev value at the start of integration so downstream systems can derive per-tick acceleration. | `playerSpawnSystem`, `characterControllerSystem`, `surfaceConstrainedVelocitySystem`, `volumetricConstrainedVelocitySystem`, `surfaceConstraintSystem` | `playerSpawnSystem`, `characterControllerSystem`, `surfaceConstrainedVelocitySystem`, `volumetricConstrainedVelocitySystem`, `surfaceConstraintSystem`, `bodyLeanSystem`, `chainDynamicsSystem`, `footPlannerSystem` |
| `forceAccumulator` | Per-entity acceleration accumulator. Force fields + controllers add into it; VelocityIntegrationSystem applies and clears each tick. | `playerSpawnSystem`, `forceFieldSystem`, `characterControllerSystem`, `surfaceConstrainedVelocitySystem`, `volumetricConstrainedVelocitySystem` | `playerSpawnSystem`, `tangentInputMapperSystem`, `forceFieldSystem`, `characterControllerSystem`, `surfaceConstrainedVelocitySystem`, `volumetricConstrainedVelocitySystem` |
| `sphereBody` | Sphere collision/visual primitive. The character is a sphere; future bodies (capsule etc) get their own buffers. | `playerSpawnSystem` | `playerSpawnSystem`, `characterRenderSyncSystem` |
| `characterInput` | Per-character input state: normalized move axes, jump edges + hold duration, camera yaw frame. Filled by CharacterInputSystem from InputBuffer + CameraBuffer; read by CharacterControllerSystem. | `playerSpawnSystem`, `characterInputSystem` | `playerSpawnSystem`, `characterInputSystem`, `tangentInputMapperSystem`, `characterControllerSystem` |
| `characterTangentInput` | Per-character surface-tangent projection of input: orthonormal tangent basis (forwardTangent, rightTangent) at the character's surface sample, plus desired-velocity scalars (vDesF, vDesR) along those axes. Written by TangentInputMapperSystem for surface-attached characters; read by CharacterControllerSystem. | `tangentInputMapperSystem` | `tangentInputMapperSystem`, `characterControllerSystem` |
| `characterController` | Per-character FSM state (surfaceRun/airborne/jump/etc), locomotion mode, profile reference, and the last transition reason for the debug HUD. | `playerSpawnSystem`, `characterOrientationSystem`, `characterControllerSystem`, `surfaceConstraintSystem`, `bodyLeanSystem` | `hudSystem`, `playerSpawnSystem`, `characterInputSystem`, `tangentInputMapperSystem`, `characterOrientationSystem`, `forceFieldSystem`, `characterControllerSystem`, `surfaceConstrainedVelocitySystem`, `volumetricConstrainedVelocitySystem`, `surfaceConstraintSystem`, `cameraPivotSystem`, `bodyLeanSystem`, `footPlannerSystem`, `footIkSystem` |
| `characterControllerProfile` | Tunable movement parameters by profile id. Edit values here to retune feel without touching code; multiple profiles can coexist for different character classes. | — | `playerSpawnSystem`, `tangentInputMapperSystem`, `characterOrientationSystem`, `forceFieldSystem`, `characterControllerSystem`, `surfaceConstrainedVelocitySystem`, `surfaceConstraintSystem`, `bodyLeanSystem`, `footPlannerSystem`, `footIkSystem` |
| `surfaceAttachment` | Per-entity surface-constraint state: which surface, UV on it, offset along normal, cached sample. Maintained by SurfaceConstraintSystem when CharacterController.locomotionMode is surfaceConstrained. | `playerSpawnSystem`, `surfaceConstrainedVelocitySystem`, `surfaceConstraintSystem` | `playerSpawnSystem`, `tangentInputMapperSystem`, `characterControllerSystem`, `surfaceConstrainedVelocitySystem`, `surfaceConstraintSystem`, `bodyLeanSystem`, `footIkSystem` |
| `surfaceProvider` | Active SurfaceProvider instances. V1 has one slot (the per-scene heightmap surface); HeightmapSystem populates it during Rebuilding. | `surfaceProviderSystem`, `parametricSurfaceSystem` | `playerSpawnSystem`, `characterControllerSystem`, `surfaceConstrainedVelocitySystem`, `surfaceConstraintSystem`, `footPlannerSystem` |
| `volumeField` | Universal gravity vector + a list of named gravity volumes (sphere/cylinder/AABB shapes carrying constant or radial-to-axis fields). ForceFieldSystem picks the highest-priority volume containing each entity per tick, or falls back to universal gravity if none match. Scene-author-declared via SceneFile.gravityVolumes. | `parametricSurfaceSystem` | `parametricSurfaceSystem`, `forceFieldSystem`, `cameraPivotSystem`, `bodyLeanSystem` |
| `rigDefinition` | Skeletal-rig archetypes (bind pose, parent topology, named bone slots). Pre-populated with 'biped'; future archetypes write additional entries. Read by SkeletonWorldSystem for topology and by spawn code for bind-pose initialization. | — | `playerSpawnSystem`, `bodyLeanSystem`, `chainDynamicsSystem`, `footPlannerSystem`, `footIkSystem`, `skeletonWorldSystem`, `skeletonDebugRenderSystem` |
| `skeleton` | Per-entity skeletal pose: bind-pose-initialized BoneState[] with current local + world transforms. Read+written by SkeletonWorldSystem (FK), later by SpineChainSystem (Phase 1B) and IKSolverSystem (Phase 1C); read by SkeletonDebugRenderSystem. | `playerSpawnSystem`, `bodyLeanSystem`, `chainDynamicsSystem`, `footIkSystem`, `skeletonWorldSystem` | `playerSpawnSystem`, `bodyLeanSystem`, `chainDynamicsSystem`, `footPlannerSystem`, `footIkSystem`, `skeletonWorldSystem`, `skeletonDebugRenderSystem` |
| `footLock` | Per-entity, per-leg foot-lock state machine. Each foot stays planted at a world position until the hip drifts beyond a threshold or velocity drops enough that the foot needs to move under the hip for balance — then it swings to a new plant. Written by FootPlannerSystem; read by FootIKSystem. | `footPlannerSystem` | `footPlannerSystem`, `footIkSystem` |

## Systems

| id | description | reads | writes | ordering |
| --- | --- | --- | --- | --- |
| `stateMachineSystem` | Runtime state machine: drives activeGraph from EventBuffer. | `events`, `stateMachine` | `events`, `stateMachine` | — |
| `inputSystem` | Drains accumulated keyboard + mouse + pointer-lock state into InputBuffer each tick, and polls navigator.getGamepads() for the primary Standard-mapping gamepad. | `input` | `input` | — |
| `inputMapperSystem` | Translates raw InputBuffer (keys, mouse, gamepad) into device-agnostic InputMapBuffer (moveAxis, lookDelta, actions). Owns the binding table, blends device sources, computes button edges, and drains mouse deltas. | `input`, `inputMap` | `input`, `inputMap` | after: `stateMachineSystem`, `inputSystem` |
| `loadSceneSystem` | Async-fetches the scene PNG + JSON when state=Loading; emits RebuildRequested when the bytes arrive. | `stateMachine`, `events` | `events`, `timing` | after: `stateMachineSystem` |
| `renderSystem` | Mirrors CameraBuffer into THREE.PerspectiveCamera and calls renderer.render(scene, camera). | `camera`, `renderRefs` | — | after: `cameraOrbitSystem`, `characterRenderSyncSystem`, `terrainMeshSystem`, `assetPlacementSystem` |
| `minimapSystem` | Draws the source bitmap with camera position + FOV wedge into an overlay canvas. | `camera`, `worldData`, `renderRefs` | `renderRefs` | after: `cameraOrbitSystem`, `renderSystem` |
| `hudSystem` | Renders a rolling transition-log toast (max 3 messages, ~2.5s lifetime each) for the player character. H toggles visibility. DOM-diffs to prevent flicker; otherwise silent when no recent transitions. | `renderRefs`, `characterController`, `inputMap` | — | after: `stateMachineSystem`, `characterControllerSystem`, `surfaceConstraintSystem`, `bodyLeanSystem`, `characterOrientationSystem`, `playerSpawnSystem`, `inputMapperSystem`, `minimapSystem` |
| `parseBitmapSystem` | Parses the rebuild bitmap into a LabelMap; writes WorldDataBuffer.{labelMap, image, sceneName}. | `stateMachine` | `worldData`, `timing` | after: `stateMachineSystem` |
| `splitLayersSystem` | Splits LabelMap into terrainMap + assetMap; majority-fills terrain under asset pixels. | `stateMachine`, `worldData` | `worldData`, `timing` | after: `parseBitmapSystem`, `stateMachineSystem` |
| `jfaSystem` | GPU jump-flood per terrain → distance field + boundary gradient. Disposes prior result. | `stateMachine`, `worldData`, `renderRefs` | `worldData`, `timing` | after: `splitLayersSystem`, `heightmapSystem`, `stateMachineSystem` |
| `heightmapSystem` | Builds a Float32 heightmap from terrainMap (per-pixel elevation + small blur + jitter). | `stateMachine`, `worldData` | `worldData`, `timing` | after: `splitLayersSystem`, `stateMachineSystem` |
| `terrainMeshSystem` | Builds the terrain mesh from heightmap + JFA splat inputs; replaces the prior mesh. | `stateMachine`, `worldData`, `renderRefs` | `renderRefs`, `timing` | after: `heightmapSystem`, `jfaSystem`, `stateMachineSystem` |
| `assetPlacementSystem` | Connected components → footprints → InstancedMesh placements. Disposes prior meshes; emits WorldReady; sets initial camera spawn on first rebuild. | `stateMachine`, `worldData`, `renderRefs`, `camera`, `events` | `renderRefs`, `camera`, `timing`, `events` | after: `terrainMeshSystem`, `stateMachineSystem` |
| `surfaceProviderSystem` | Wraps the per-scene heightmap in a HeightmapSurfaceProvider and writes it into SurfaceProviderBuffer for character systems to consume. | `stateMachine`, `worldData` | `surfaceProvider`, `timing` | after: `heightmapSystem`, `assetPlacementSystem`, `stateMachineSystem` |
| `parametricSurfaceSystem` | When scene.parametric is set, builds the analytic SurfaceProvider + a visualization mesh, replaces any existing terrain mesh in the THREE scene, and writes the provider into SurfaceProviderBuffer. | `stateMachine`, `worldData`, `renderRefs`, `volumeField` | `worldData`, `surfaceProvider`, `renderRefs`, `volumeField`, `timing` | after: `stateMachineSystem`, `surfaceProviderSystem`, `terrainMeshSystem`, `assetPlacementSystem`; before: `renderSystem` |
| `playerSpawnSystem` | Spawns the player entity on first rebuild and re-attaches to the new heightmap on subsequent rebuilds. Sets transform from a south-edge spawn UV. | `stateMachine`, `worldData`, `surfaceProvider`, `entity`, `transform`, `velocity`, `forceAccumulator`, `sphereBody`, `characterController`, `characterInput`, `characterControllerProfile`, `surfaceAttachment`, `rigDefinition`, `skeleton` | `entity`, `transform`, `velocity`, `forceAccumulator`, `sphereBody`, `characterController`, `characterInput`, `surfaceAttachment`, `skeleton`, `timing` | after: `surfaceProviderSystem`, `parametricSurfaceSystem`, `stateMachineSystem` |
| `builderInputSystem` | Placeholder for the DOM-event pump that feeds BuilderSystem. No buffer access. | — | — | after: `stateMachineSystem` |
| `builderSystem` | Mutates BuilderBuffer in response to BuilderEvent[] from the DOM. Bootstraps the buffer on first activation. Rebuilds palette/asset-menu DOM when state changes. Emits RebuildRequested on Send-to-World. | `stateMachine`, `builder`, `worldData`, `events` | `builder`, `events`, `timing` | after: `stateMachineSystem`, `builderInputSystem` |
| `characterInputSystem` | Reads semantic InputMapBuffer + CameraBuffer; writes per-character move axes, jump edges, and camera yaw into CharacterInputBuffer. | `inputMap`, `camera`, `characterController`, `characterInput` | `characterInput` | after: `stateMachineSystem`, `inputMapperSystem` |
| `tangentInputMapperSystem` | Projects per-character input + camera yaw onto the surface tangent plane. Writes the orthonormal tangent basis (forwardTangent, rightTangent) and desired-velocity scalars (vDesF, vDesR) into CharacterTangentInputBuffer for every surface-attached character. CharacterControllerSystem consumes this instead of recomputing the projection itself. | `characterInput`, `characterController`, `characterControllerProfile`, `surfaceAttachment`, `forceAccumulator`, `characterTangentInput` | `characterTangentInput` | after: `stateMachineSystem`, `characterInputSystem`, `characterOrientationSystem`, `forceFieldSystem` |
| `characterOrientationSystem` | Second-order body yaw controller. Latches targetYaw to camera yaw only when look input is active (mouse delta or right stick); chases via desired-velocity + capped-acceleration phases. Writes Transform.yaw and CharacterController.{yawVel, targetYaw}. | `inputMap`, `camera`, `characterControllerProfile`, `characterController`, `transform` | `characterController`, `transform` | after: `stateMachineSystem`, `characterInputSystem`; before: `forceFieldSystem`, `characterControllerSystem` |
| `forceFieldSystem` | Per-entity gravity picker: reads VolumeFieldBuffer (universal gravity + scene-author volumes), TransformBuffer for entity positions, and CharacterControllerProfileBuffer for glideGravityMul. Writes the picked gravity vector into ForceAccumulatorBuffer for every character entity each tick. | `volumeField`, `transform`, `characterController`, `characterControllerProfile`, `forceAccumulator` | `forceAccumulator` | after: `stateMachineSystem`, `characterInputSystem` |
| `characterControllerSystem` | Per-character FSM. In surface mode, runs the surface-frame solver (project to tangent/normal basis, cap tangent control by min(profile, grip), add surface-reaction normal to absorb gravity-into-surface). Accumulates accelerations; jump impulses still write velocity directly. | `characterInput`, `characterTangentInput`, `characterControllerProfile`, `surfaceAttachment`, `surfaceProvider`, `transform`, `characterController`, `velocity`, `forceAccumulator` | `characterController`, `velocity`, `forceAccumulator` | after: `stateMachineSystem`, `characterInputSystem`, `tangentInputMapperSystem`, `forceFieldSystem` |
| `surfaceConstrainedVelocitySystem` | UV-space integration for surface-attached characters. Projects world velocity & accel onto the surface tangent frame, integrates in UV via semi-implicit Euler, derives new world position from the integrated UV (sample + radius·N), and reconstructs world velocity from the new tangent frame. Eliminates the XYZ-then-snap pattern and its bug class (mesa-snap, cliff-snap, cylinder-embed). | `characterController`, `characterControllerProfile`, `surfaceProvider`, `surfaceAttachment`, `forceAccumulator`, `velocity`, `transform` | `surfaceAttachment`, `forceAccumulator`, `velocity`, `transform` | after: `stateMachineSystem`, `characterControllerSystem` |
| `volumetricConstrainedVelocitySystem` | Integrates airborne characters and non-character entities (any moving entity that is NOT locomotionMode === 'surfaceConstrained'). Semi-implicit Euler in XYZ. Runs after SurfaceConstrainedVelocitySystem to disambiguate shared-buffer ordering. | `characterController`, `forceAccumulator`, `velocity`, `transform` | `forceAccumulator`, `velocity`, `transform` | after: `stateMachineSystem`, `characterControllerSystem`, `surfaceConstrainedVelocitySystem` |
| `surfaceConstraintSystem` | Surface-attach/detach event handler. For surface-attached entities: detects walked-off-edge from out-of-bounds UV written by SurfaceConstrainedVelocity, transitions to airborne. For airborne entities: detects landing (body crosses below groundY while descending), snaps to surface and re-attaches. | `surfaceProvider`, `characterControllerProfile`, `characterController`, `surfaceAttachment`, `transform`, `velocity` | `characterController`, `surfaceAttachment`, `transform`, `velocity` | after: `stateMachineSystem`, `surfaceConstrainedVelocitySystem`, `volumetricConstrainedVelocitySystem` |
| `cameraPivotSystem` | Writes CameraBuffer.pivot — orbit centre + local frame. Samples local gravity via volumeField for pivot.up, parallel-transports pivot.fwd each tick, and exponentially chases position/up/fwd toward their targets at pivotResponsiveness. | `transform`, `characterController`, `volumeField`, `camera` | `camera` | after: `stateMachineSystem`, `surfaceConstraintSystem`, `forceFieldSystem` |
| `cameraOrbitSystem` | Third-person spherical orbit camera around CameraBuffer.pivot. Reads InputMapBuffer.lookDelta + pivot frame, writes pos/yaw/pitch/target. Uses local-pitch as elevation above pivot.up (sign-flipped mouse-pitch: mouse-down lifts camera). | `inputMap`, `camera` | `camera` | after: `stateMachineSystem`, `inputMapperSystem`, `characterInputSystem`, `cameraPivotSystem` |
| `characterRenderSyncSystem` | Mirrors entity Transform + SphereBody into Three.js meshes added to the active scene. Visible-character sync; no logic. | `transform`, `sphereBody`, `renderRefs` | — | after: `stateMachineSystem`, `surfaceConstraintSystem` |
| `bodyLeanSystem` | Apparent-gravity body lean solver. Computes target body-up from gravity, velocity, accel and surface normal; smooths via slerp into CharacterController.bodyUpCurrent; writes pelvis localRot and pelvis compression. Pelvis owner; ChainDynamicsSystem animates spine bones only. | `transform`, `velocity`, `rigDefinition`, `surfaceAttachment`, `volumeField`, `characterControllerProfile`, `characterController`, `skeleton` | `characterController`, `skeleton` | after: `stateMachineSystem`, `surfaceConstraintSystem`, `cameraOrbitSystem`; before: `chainDynamicsSystem`, `skeletonWorldSystem` |
| `chainDynamicsSystem` | Spring-damped chain lean (Wolfire-style). For each rig.chains entry, distributes a velocity-driven target lean across the chain segments and integrates per-bone leanVec/leanVel; writes derived localRot. Generic across spine/tail/neck/quadruped chains. | `rigDefinition`, `transform`, `velocity`, `skeleton` | `skeleton` | after: `stateMachineSystem`, `surfaceConstraintSystem`; before: `skeletonWorldSystem` |
| `footPlannerSystem` | Per-foot plant-and-step planner. Feet remain locked to a world position until hip translation or body yaw passes profile thresholds, then swing (one at a time) to a predicted future-hip plant. Reads transform/velocity/surface; writes FootLockBuffer for FootIKSystem to consume. | `rigDefinition`, `transform`, `velocity`, `surfaceProvider`, `characterController`, `characterControllerProfile`, `skeleton`, `footLock` | `footLock` | after: `stateMachineSystem`, `surfaceConstraintSystem`, `cameraOrbitSystem`, `chainDynamicsSystem` |
| `footIkSystem` | Per-leg 2-bone IK. Surface-constrained: reads FootLockBuffer for plant + swing. Volume-constrained: body-relative airborne pose. Writes upperLeg/lowerLeg localRots. | `rigDefinition`, `transform`, `characterController`, `characterControllerProfile`, `footLock`, `surfaceAttachment`, `skeleton` | `skeleton` | after: `stateMachineSystem`, `chainDynamicsSystem`, `footPlannerSystem`; before: `skeletonWorldSystem` |
| `skeletonWorldSystem` | Forward-kinematics pass: composes per-bone world transforms from entity transform + per-bone local transforms. Walks each bone in parent-index order (parent < self invariant). | `rigDefinition`, `transform`, `skeleton` | `skeleton` | after: `stateMachineSystem`, `surfaceConstraintSystem` |
| `skeletonDebugRenderSystem` | Debug-renders each entity's skeleton as white THREE.LineSegments (parent→child). Closure-cached one mesh per entity; depthTest disabled so bones show through the physics body. Swappable for a skinned-mesh renderer at Phase 1.5 without touching compute systems. | `skeleton`, `rigDefinition`, `renderRefs` | — | after: `skeletonWorldSystem`, `characterRenderSyncSystem`; before: `renderSystem` |

## How to use this index

Before adding a new buffer or system, search this file for an existing entry
that already covers your need. The runtime won't stop you from adding
duplicates, but the result will be more code to maintain and conflicting
sources of truth.

If you do add something new:
1. Update the relevant file in `src/buffers/` or `src/systems/`.
2. Run `npm run registry`.
3. Add a test in `tests/buffers/` or `tests/systems/`.
