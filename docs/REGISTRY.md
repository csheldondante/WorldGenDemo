# Runtime Registry

> **This file is generated by `npm run registry`. Do not edit by hand.**
>
> Source of truth: `src/buffers/` and `src/systems/`. Re-run the script
> after adding or modifying any buffer or system.

## Buffers

| id | description | written by | read by |
| --- | --- | --- | --- |
| `input` | Held keys, accumulated mouse deltas, pointer-lock state. Cleared each tick by InputSystem. | `inputSystem`, `cameraFollowSystem` | `inputSystem`, `characterInputSystem`, `cameraFollowSystem` |
| `camera` | Source-of-truth camera state. RenderSystem mirrors this into THREE.PerspectiveCamera each frame. | `assetPlacementSystem`, `cameraFollowSystem` | `renderSystem`, `minimapSystem`, `hudSystem`, `assetPlacementSystem`, `characterInputSystem`, `cameraFollowSystem` |
| `events` | FIFO event queue drained each tick by StateMachineSystem. | `stateMachineSystem`, `loadSceneSystem`, `assetPlacementSystem`, `builderSystem` | `stateMachineSystem`, `loadSceneSystem`, `assetPlacementSystem`, `builderSystem` |
| `stateMachine` | Active runtime state + the graph id the scheduler should run this tick. | `stateMachineSystem` | `stateMachineSystem`, `loadSceneSystem`, `hudSystem`, `parseBitmapSystem`, `splitLayersSystem`, `jfaSystem`, `heightmapSystem`, `terrainMeshSystem`, `assetPlacementSystem`, `surfaceProviderSystem`, `playerSpawnSystem`, `builderSystem` |
| `renderRefs` | Three.js handles + DOM refs. Three.js is a render backend; gameplay state lives in other buffers. | `minimapSystem`, `terrainMeshSystem`, `assetPlacementSystem` | `renderSystem`, `minimapSystem`, `hudSystem`, `jfaSystem`, `terrainMeshSystem`, `assetPlacementSystem`, `characterRenderSyncSystem` |
| `worldData` | Per-scene data: source bitmap, parsed maps, heightmap, JFA outputs. Each pipeline stage writes its slice. | `parseBitmapSystem`, `splitLayersSystem`, `jfaSystem`, `heightmapSystem` | `minimapSystem`, `hudSystem`, `splitLayersSystem`, `jfaSystem`, `heightmapSystem`, `terrainMeshSystem`, `assetPlacementSystem`, `surfaceProviderSystem`, `playerSpawnSystem`, `builderSystem` |
| `timing` | Per-stage timing metrics + warnings. Read by HudSystem; written by every system that times itself. | `loadSceneSystem`, `parseBitmapSystem`, `splitLayersSystem`, `jfaSystem`, `heightmapSystem`, `terrainMeshSystem`, `assetPlacementSystem`, `surfaceProviderSystem`, `playerSpawnSystem`, `builderSystem` | `hudSystem` |
| `builder` | Mode-bounded editor state: bitmap, palette of {kind,id,color} entries, active brush, undo history, thumbnails. Lifetime is the editor session, not per-scene. | `builderSystem` | `builderSystem` |
| `entity` | Lightweight ECS: entities are integer IDs in `alive`; components live in keyed buffers (transform, velocity, characterController, etc). | `playerSpawnSystem` | `playerSpawnSystem` |
| `transform` | Per-entity world position, yaw, scale. Source of truth for character + asset placement. | `playerSpawnSystem`, `velocityIntegrationSystem`, `surfaceConstraintSystem` | `playerSpawnSystem`, `characterControllerSystem`, `velocityIntegrationSystem`, `surfaceConstraintSystem`, `cameraFollowSystem`, `characterRenderSyncSystem` |
| `velocity` | Per-entity world-space linear velocity (m/s). VelocityIntegrationSystem updates Transform from this each tick. | `playerSpawnSystem`, `characterControllerSystem`, `velocityIntegrationSystem`, `surfaceConstraintSystem` | `playerSpawnSystem`, `characterControllerSystem`, `velocityIntegrationSystem`, `surfaceConstraintSystem` |
| `forceAccumulator` | Per-entity acceleration accumulator. Force fields + controllers add into it; VelocityIntegrationSystem applies and clears each tick. | `playerSpawnSystem`, `forceFieldSystem`, `velocityIntegrationSystem` | `playerSpawnSystem`, `forceFieldSystem`, `velocityIntegrationSystem` |
| `sphereBody` | Sphere collision/visual primitive. The character is a sphere; future bodies (capsule etc) get their own buffers. | `playerSpawnSystem` | `playerSpawnSystem`, `characterRenderSyncSystem` |
| `characterInput` | Per-character input state: normalized move axes, jump edges + hold duration, camera yaw frame. Filled by CharacterInputSystem from InputBuffer + CameraBuffer; read by CharacterControllerSystem. | `playerSpawnSystem`, `characterInputSystem` | `playerSpawnSystem`, `characterInputSystem`, `characterControllerSystem` |
| `characterController` | Per-character FSM state (surfaceRun/airborne/jump/etc), locomotion mode, profile reference, and the last transition reason for the debug HUD. | `playerSpawnSystem`, `characterInputSystem`, `characterControllerSystem`, `surfaceConstraintSystem` | `playerSpawnSystem`, `characterInputSystem`, `forceFieldSystem`, `characterControllerSystem`, `surfaceConstraintSystem`, `cameraFollowSystem` |
| `characterControllerProfile` | Tunable movement parameters by profile id. Edit values here to retune feel without touching code; multiple profiles can coexist for different character classes. | — | `playerSpawnSystem`, `forceFieldSystem`, `characterControllerSystem`, `surfaceConstraintSystem` |
| `surfaceAttachment` | Per-entity surface-constraint state: which surface, UV on it, offset along normal, cached sample. Maintained by SurfaceConstraintSystem when CharacterController.locomotionMode is surfaceConstrained. | `playerSpawnSystem`, `surfaceConstraintSystem` | `playerSpawnSystem`, `characterControllerSystem`, `surfaceConstraintSystem` |
| `surfaceProvider` | Active SurfaceProvider instances. V1 has one slot (the per-scene heightmap surface); HeightmapSystem populates it during Rebuilding. | `surfaceProviderSystem` | `playerSpawnSystem`, `characterControllerSystem`, `surfaceConstraintSystem` |
| `volumeField` | Force fields acting on volume-constrained entities. V1 has just one constant-downward gravity vector. Future: arrays of localized fields (wind, low-G zones, etc). | — | `forceFieldSystem` |

## Systems

| id | description | reads | writes | ordering |
| --- | --- | --- | --- | --- |
| `stateMachineSystem` | Runtime state machine: drives activeGraph from EventBuffer. | `events`, `stateMachine` | `events`, `stateMachine` | — |
| `inputSystem` | Drains accumulated keyboard + mouse + pointer-lock state into InputBuffer each tick. | `input` | `input` | — |
| `loadSceneSystem` | Async-fetches the scene PNG + JSON when state=Loading; emits RebuildRequested when the bytes arrive. | `stateMachine`, `events` | `events`, `timing` | after: `stateMachineSystem` |
| `renderSystem` | Mirrors CameraBuffer into THREE.PerspectiveCamera and calls renderer.render(scene, camera). | `camera`, `renderRefs` | — | after: `cameraFollowSystem`, `characterRenderSyncSystem`, `terrainMeshSystem`, `assetPlacementSystem` |
| `minimapSystem` | Draws the source bitmap with camera position + FOV wedge into an overlay canvas. | `camera`, `worldData`, `renderRefs` | `renderRefs` | after: `cameraFollowSystem`, `renderSystem` |
| `hudSystem` | Renders timings, scene name, FSM state, and camera pos into the HUD overlay. | `camera`, `timing`, `stateMachine`, `worldData`, `renderRefs` | — | after: `stateMachineSystem`, `cameraFollowSystem`, `minimapSystem`, `loadSceneSystem`, `parseBitmapSystem`, `splitLayersSystem`, `jfaSystem`, `heightmapSystem`, `terrainMeshSystem`, `assetPlacementSystem`, `surfaceProviderSystem`, `playerSpawnSystem`, `builderSystem` |
| `parseBitmapSystem` | Parses the rebuild bitmap into a LabelMap; writes WorldDataBuffer.{labelMap, image, sceneName}. | `stateMachine` | `worldData`, `timing` | after: `stateMachineSystem` |
| `splitLayersSystem` | Splits LabelMap into terrainMap + assetMap; majority-fills terrain under asset pixels. | `stateMachine`, `worldData` | `worldData`, `timing` | after: `parseBitmapSystem`, `stateMachineSystem` |
| `jfaSystem` | GPU jump-flood per terrain → distance field + boundary gradient. Disposes prior result. | `stateMachine`, `worldData`, `renderRefs` | `worldData`, `timing` | after: `splitLayersSystem`, `heightmapSystem`, `stateMachineSystem` |
| `heightmapSystem` | Builds a Float32 heightmap from terrainMap (per-pixel elevation + small blur + jitter). | `stateMachine`, `worldData` | `worldData`, `timing` | after: `splitLayersSystem`, `stateMachineSystem` |
| `terrainMeshSystem` | Builds the terrain mesh from heightmap + JFA splat inputs; replaces the prior mesh. | `stateMachine`, `worldData`, `renderRefs` | `renderRefs`, `timing` | after: `heightmapSystem`, `jfaSystem`, `stateMachineSystem` |
| `assetPlacementSystem` | Connected components → footprints → InstancedMesh placements. Disposes prior meshes; emits WorldReady; sets initial camera spawn on first rebuild. | `stateMachine`, `worldData`, `renderRefs`, `camera`, `events` | `renderRefs`, `camera`, `timing`, `events` | after: `terrainMeshSystem`, `stateMachineSystem` |
| `surfaceProviderSystem` | Wraps the per-scene heightmap in a HeightmapSurfaceProvider and writes it into SurfaceProviderBuffer for character systems to consume. | `stateMachine`, `worldData` | `surfaceProvider`, `timing` | after: `heightmapSystem`, `assetPlacementSystem`, `stateMachineSystem` |
| `playerSpawnSystem` | Spawns the player entity on first rebuild and re-attaches to the new heightmap on subsequent rebuilds. Sets transform from a south-edge spawn UV. | `stateMachine`, `worldData`, `surfaceProvider`, `entity`, `transform`, `velocity`, `forceAccumulator`, `sphereBody`, `characterController`, `characterInput`, `characterControllerProfile`, `surfaceAttachment` | `entity`, `transform`, `velocity`, `forceAccumulator`, `sphereBody`, `characterController`, `characterInput`, `surfaceAttachment`, `timing` | after: `surfaceProviderSystem`, `stateMachineSystem` |
| `builderInputSystem` | Placeholder for the DOM-event pump that feeds BuilderSystem. No buffer access. | — | — | after: `stateMachineSystem` |
| `builderSystem` | Mutates BuilderBuffer in response to BuilderEvent[] from the DOM. Bootstraps the buffer on first activation. Rebuilds palette/asset-menu DOM when state changes. Emits RebuildRequested on Send-to-World. | `stateMachine`, `builder`, `worldData`, `events` | `builder`, `events`, `timing` | after: `stateMachineSystem`, `builderInputSystem` |
| `characterInputSystem` | Reads raw InputBuffer + CameraBuffer; writes per-character normalized move axes and jump edges into CharacterInputBuffer. | `input`, `camera`, `characterController`, `characterInput` | `characterController`, `characterInput` | after: `stateMachineSystem`, `inputSystem` |
| `forceFieldSystem` | Reads VolumeFieldBuffer.gravity, writes accelerations into ForceAccumulatorBuffer for volume-constrained entities (with glide multiplier when state==glide). | `volumeField`, `characterController`, `characterControllerProfile`, `forceAccumulator` | `forceAccumulator` | after: `stateMachineSystem`, `characterInputSystem` |
| `characterControllerSystem` | Per-character FSM. Applies desired velocity via acceleration profile; emits jump impulses; evaluates slope/landing threshold transitions. | `characterInput`, `characterControllerProfile`, `surfaceAttachment`, `surfaceProvider`, `transform`, `characterController`, `velocity` | `characterController`, `velocity` | after: `stateMachineSystem`, `characterInputSystem`, `forceFieldSystem` |
| `velocityIntegrationSystem` | Semi-implicit Euler integration: v += a·dt; pos += v·dt. Clears the force accumulator after. | `forceAccumulator`, `velocity`, `transform` | `forceAccumulator`, `velocity`, `transform` | after: `stateMachineSystem`, `characterControllerSystem` |
| `surfaceConstraintSystem` | Snaps surface-attached entities to the surface y. Detects landings: volume-constrained entities reattach when descending into the surface. | `surfaceProvider`, `characterControllerProfile`, `characterController`, `surfaceAttachment`, `transform`, `velocity` | `characterController`, `surfaceAttachment`, `transform`, `velocity` | after: `stateMachineSystem`, `velocityIntegrationSystem` |
| `cameraFollowSystem` | Third-person follow camera. Reads InputBuffer (mouse), TransformBuffer (player), writes CameraBuffer pos/yaw/pitch behind+above the player. | `input`, `transform`, `characterController`, `camera` | `input`, `camera` | after: `stateMachineSystem`, `surfaceConstraintSystem`, `inputSystem`, `characterInputSystem` |
| `characterRenderSyncSystem` | Mirrors entity Transform + SphereBody into Three.js meshes added to the active scene. Visible-character sync; no logic. | `transform`, `sphereBody`, `renderRefs` | — | after: `stateMachineSystem`, `surfaceConstraintSystem` |

## How to use this index

Before adding a new buffer or system, search this file for an existing entry
that already covers your need. The runtime won't stop you from adding
duplicates, but the result will be more code to maintain and conflicting
sources of truth.

If you do add something new:
1. Update the relevant file in `src/buffers/` or `src/systems/`.
2. Run `npm run registry`.
3. Add a test in `tests/buffers/` or `tests/systems/`.
